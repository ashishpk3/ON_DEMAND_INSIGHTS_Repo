# Copy patterns — Render injects PORT automatically when using Dockerfile CMD.

# Strongly recommended whenever the service is on the public internet (team-wide agent).
ON_DEMAND_INSIGHTS_API_KEY=

# Local development (optional overrides — see README uvicorn command).
# INSIGHTS_HOST=127.0.0.1
# INSIGHTS_PORT=8890
